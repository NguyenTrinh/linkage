* Revamp plugins
* Mathusalem plugin? http://tw.apinc.org/weblog/2006/08/13#mathusalem-gets-useful
* Update TorrentCreator (private flag, DHT, multi tracker)
* Make plugins configureable
* Add RSS, Scheduler and Auto-file filter (like *.nfo etc...) plugins
* Fix dbus stuff, as it is now it's an ugly mockup
* Labels: replace wasted with seen copies, add total done to files tab
* Add option to trunkate long torrent names
* Make it possible to add user-defined trackers
